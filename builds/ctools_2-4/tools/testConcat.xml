<project name="testConcat" basedir="." default="buildPropertiesFileConcat">

  <property name="propertyfile.dir" value="." />
  <property name="propertyfile.name" value="testConcat.properties" />
  <property file="${propertyfile.dir}/${propertyfile.name}" />
  <property name="instances.dir" value="/Users/dlhaines/tmp/test-concat" />
  <property name="instance-type" value="JAN" />
  <property name="inputfiles.dir" value="input1" />

  <!-- see if can get list of files from properties file -->

  <target name="buildPropertiesFileConcat"> 
    <concat destfile="${instances.dir}/${instance-type}/sakai.properties">
      <filelist dir="${inputfiles.dir}" files="${propertiesfileslist}" />
    </concat>
  </target>

</project>
