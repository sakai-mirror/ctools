<project name="build-type-dummy" basedir="." default="info" >

  <!-- 
  This is a default build file that lists the targets that need to be
  overridden to create a useful build file.
  -->

<target name="info">	
		<echo> build-type-dummy info target </echo>
</target>
	
	
  <target name="buildCToolsImage">
	<fail>
	
  	Must specify a value for type on the command line.
	e.g. ant -Dtype=production buildCToolsImage

	</fail>
  </target>

  <target name="updateConfigurationSource" />
  <target name="getConfigurationSrc" />
  <target name="modifyImage" />
  <target name="getSource" />
  <target name="editSource" />
  <target name="compileSakai" />
  <target name="buildConfig" />

</project>